<!doctype html>
<html lang="ko">
<head>
    <meta charset="utf-8">
    <title>{{title}}</title>
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body class="bg-light">
<nav class="navbar navbar-dark bg-dark">
    <div class="container">
        <a class="navbar-brand" href="/guarantees/list">USAFE Guarantee</a>
        <div>
            <a class="btn btn-sm btn-secondary" href="/guarantees/list">목록으로</a>
        </div>
    </div>
</nav>

<main class="container py-4">
    <div class="row justify-content-center">
        <div class="col-md-6">
            <div class="card">
                <div class="card-header">
                    <h4 class="mb-0">📝 보증서 수정</h4>
                    <small class="text-muted">보증서 ID: {{guarantee.id}} | 보증서번호: {{guarantee.guaranteeNumber}}</small>
                </div>
                <div class="card-body">
                    {{#errors}}
                        <div class="alert alert-danger">
                            <ul class="mb-0">
                                {{#.}}<li>{{.}}</li>{{/.}}
                            </ul>
                        </div>
                    {{/errors}}

                    <form method="post" action="/guarantees/{{guarantee.id}}">
                        <div class="mb-3">
                            <label for="orderId" class="form-label">주문번호 *</label>
                            <input type="text" class="form-control" id="orderId" name="orderId" 
                                   value="{{form.orderId}}" required placeholder="주문번호를 입력하세요">
                        </div>

                        <div class="mb-3">
                            <label for="buyerName" class="form-label">구매자명 *</label>
                            <input type="text" class="form-control" id="buyerName" name="buyerName" 
                                   value="{{form.buyerName}}" required placeholder="구매자명을 입력하세요">
                        </div>

                        <div class="mb-3">
                            <label for="amount" class="form-label">보증금액 *</label>
                            <div class="input-group">
                                <input type="number" class="form-control" id="amount" name="amount" 
                                       value="{{form.amount}}" required min="0" step="0.01" placeholder="0.00">
                                <span class="input-group-text">원</span>
                            </div>
                        </div>

                        <div class="mb-3">
                            <label class="form-label">현재 상태</label>
                            <div class="form-control-plaintext">
                                {{#guarantee.status}}
                                    {{#ISSUED}}<span class="badge bg-success fs-6">발급완료</span>{{/ISSUED}}
                                    {{#CANCELLED}}<span class="badge bg-danger fs-6">취소됨</span>{{/CANCELLED}}
                                    {{^ISSUED}}{{^CANCELLED}}<span class="badge bg-secondary fs-6">{{guarantee.status}}</span>{{/CANCELLED}}{{/ISSUED}}
                                {{/guarantee.status}}
                            </div>
                            <small class="text-muted">발급일시: {{guarantee.issuedAt}}</small>
                            {{#guarantee.updatedAt}}
                            <br><small class="text-muted">수정일시: {{guarantee.updatedAt}}</small>
                            {{/guarantee.updatedAt}}
                        </div>

                        <div class="d-grid gap-2 d-md-flex justify-content-md-end">
                            <a href="/guarantees/list" class="btn btn-secondary">취소</a>
                            <button type="submit" class="btn btn-primary">수정 완료</button>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>
</main>

<footer class="text-center text-muted py-4">
    <small>Version {{version}} · © USAFE</small>
</footer>
</body>
</html>