<!doctype html>
<html lang="ko">
<head>
    <meta charset="utf-8">
    <title>{{title}}</title>
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body class="bg-light">
<nav class="navbar navbar-dark bg-dark">
    <div class="container">
        <a class="navbar-brand" href="/guarantees/list">USAFE Guarantee</a>
        <div>
            <a class="btn btn-sm btn-primary" href="/guarantees/new">보증서 발급</a>
            <a class="btn btn-sm btn-secondary ms-2" href="/api/health">헬스체크</a>
            <a class="btn btn-sm btn-secondary ms-2" href="/api/version">버전</a>
        </div>
    </div>
</nav>

<main class="container py-4">
    <div class="d-flex justify-content-between align-items-center mb-3">
        <h2>보증서 목록</h2>
        <a class="btn btn-success" href="/guarantees/new">+ 새 보증서</a>
    </div>

    {{#guarantees}}
        <table class="table table-hover align-middle">
            <thead>
            <tr><th>ID</th><th>주문번호</th><th>구매자</th><th>금액</th><th>상태</th><th>발급시각</th></tr>
            </thead>
            <tbody>
            {{#.}}
                <tr>
                    <td>{{id}}</td>
                    <td>{{orderId}}</td>
                    <td>{{buyerName}}</td>
                    <td>{{amount}}</td>
                    <td>{{status}}</td>
                    <td>{{issuedAt}}</td>
                </tr>
            {{/.}}
            </tbody>
        </table>
    {{/guarantees}}

    {{^guarantees}}
        <div class="alert alert-info">아직 발급된 보증서가 없습니다. 우측 상단 <b>+ 새 보증서</b>를 눌러 등록하세요.</div>
    {{/guarantees}}
</main>

<footer class="text-center text-muted py-4">
    <small>Version {{version}} · © USAFE</small>
</footer>
</body>
</html>
